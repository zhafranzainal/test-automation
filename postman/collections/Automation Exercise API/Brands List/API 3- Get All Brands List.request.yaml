$kind: http-request
name: "API 3: Get All Brands List"
url: "{{baseUrl}}/brandsList"
method: GET
scripts:
  - type: afterResponse
    code: "pm.test(\"Status 200\", () => {\r

      \    pm.response.to.have.status(200);\r

      });\r

      \r

      const body = pm.response.json();\r

      \r

      pm.test(\"responseCode is 200\", () => {\r

      \    pm.expect(body.responseCode).to.eql(200);\r

      });\r

      \r

      pm.test(\"brands is a non-empty array\", () => {\r

      \    pm.expect(Array.isArray(body.brands)).to.be.true;\r

      \    pm.expect(body.brands.length).to.be.greaterThan(0);\r

      });"
    language: text/javascript
order: 1000
